services:
  postgres:
    image: postgres:16
    container_name: forma-db
    environment:
      POSTGRES_DB: forma
      POSTGRES_USER: forma
      POSTGRES_PASSWORD: forma
    ports:
      - "5433:5432"
    volumes:
      - forma-data:/var/lib/postgresql/data

volumes:
  forma-data: